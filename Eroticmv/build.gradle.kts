version = 7

cloudstream {
    authors     = listOf("imperialbob")
    language    = "en"
    description = "Softcore erotic adult movies, classic and modern, full in HD on eroticmv.com"
    status      = 1
    tvTypes     = listOf("NSFW")

    iconUrl = "https://www.google.com/s2/favicons?domain=eroticmv.com&sz=%size%"
}

// unit tests get the cloudstream stub classes from the root build.gradle.kts
// (testImplementation(files(configurations.getByName("cloudstream"))) — every provider)
